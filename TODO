IGNORE THIS FILE
IT IS JUST ME DUMPING OUT MY BRAIN

?? Session Implimentation (cookie|session)
Clean up UA Library
Impliment User Auth Library in System.php

====> Custom Session Data Schema <=====
$auth['user'] = 'joe';
$auth['id'] = '32char HEX code';
$auth['mode'] = 'access';
$auth['role']['name'] = 'admin';
$auth['role']['id'] = 0;
$auth['log'] = array();
$auth['conf'] = array();

==> Protected Conf
stored into user's session_conf field in MySQL in MLDF (Multi-Level Data Format)
key%%>value[%%]key2%%>value2[%%]key3%%>value3
as such %%> and {%%} are system words

++ System Dictionary ++
MLDS -> arr[ <s> | [s] | {s} ] + arr[ % | & | # ]*2
EX* UI* UA* FS*

++ Code Structs ++
$var_name
XXFunctionName()
DefinedVar or XXDefinedVar
true, false, null, nil, yes, no
include, require, echo '';
else ();
<<<NODONTUSE

Application FS Schema
UI FS Schema
Package FS Schema

nil is not null, it is a file path if you are forced to pass one, it is blank.
Impliment a way to keep nil nil and not gain text
prob though permissions to read only
